import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = { title: "Portal do Cliente | Login" };

export default function PortalLoginPage() {
  return <LoginForm title="Portal do Cliente" subtitle="Acesse sua área exclusiva" />;
}
